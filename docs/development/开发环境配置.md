# AutonoMind 开发环境配置指南

## 1. 文档信息

| 字段 | 内容 |
|------|------|
| **文档名称** | AutonoMind 开发环境配置指南 |
| **版本** | v1.0 |
| **创建日期** | 2026-03-02 |
| **适用范围** | 开发人员 |

---

## 2. 目录

1. [环境要求](#3-环境要求)
2. [项目结构](#4-项目结构)
3. [本地开发搭建](#5-本地开发搭建)
4. [依赖安装](#6-依赖安装)
5. [数据库配置](#7-数据库配置)
6. [环境变量](#8-环境变量)
7. [启动开发服务](#9-启动开发服务)
8. [开发工具配置](#10-开发工具配置)
9. [常见问题](#11-常见问题)

---

## 3. 环境要求

### 3.1 硬件要求

| 资源 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 4核 | 8核+ |
| 内存 | 8GB | 16GB+ |
| 磁盘 | 20GB SSD | 50GB+ SSD |
| 网络 | 稳定宽带 | 稳定宽带 |

### 3.2 软件要求

| 软件 | 版本 | 说明 |
|------|------|------|
| **Python** | 3.10+ | Python 运行环境 |
| **Node.js** | 18+ | 前端开发(可选) |
| **Docker** | 20.10+ | 容器化部署 |
| **Docker Compose** | 2.0+ | 编排服务 |
| **Git** | 2.30+ | 版本控制 |
| **PostgreSQL** | 15+ | 数据库 |
| **Redis** | 7+ | 缓存 |
| **RabbitMQ** | 3.12+ | 消息队列 |

---

## 4. 项目结构

```
autonomind/
├── code/                      # 源代码
│   ├── backend/              # 后端代码
│   │   ├── app/             # 应用代码
│   │   │   ├── api/         # API 路由
│   │   │   ├── core/        # 核心模块
│   │   │   ├── models/      # 数据模型
│   │   │   ├── schemas/     # Pydantic 模型
│   │   │   └── services/    # 业务逻辑
│   │   ├── tests/           # 测试代码
│   │   └── alembic/         # 数据库迁移
│   └── frontend/            # 前端代码(可选)
│       └── src/
├── docs/                     # 文档
│   ├── adr/                 # 技术决策
│   ├── api/                 # API 文档
│   ├── architecture/        # 架构文档
│   ├── development/         # 开发文档
│   └── product/             # 产品文档
├── others/                  # 其他资源
│   ├── scripts/             # 脚本
│   └── configs/             # 配置文件
├── .env.example             # 环境变量示例
├── docker-compose.yml       # Docker 编排
├── README.md                # 项目说明
└── requirements.txt         # Python 依赖
```

---

## 5. 本地开发搭建

### 5.1 克隆项目

```bash
git clone https://github.com/your-org/autonomind.git
cd autonomind
```

### 5.2 创建虚拟环境

```bash
# 使用 venv
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 或使用 conda
conda create -n autonomind python=3.10
conda activate autonomind
```

---

## 6. 依赖安装

### 6.1 安装 Python 依赖

```bash
# 安装基础依赖
pip install -r requirements.txt

# 或使用 poetry(推荐)
poetry install
```

### 6.2 安装开发依赖

```bash
pip install -r requirements-dev.txt
```

### 6.3 requirements.txt 内容

```txt
# Web 框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# 数据库
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.0

# 向量数据库
qdrant-client==1.7.0

# 缓存
redis==5.0.1

# 消息队列
celery==5.3.4
kombu==5.3.5

# LLM
langchain==0.1.0
openai==1.6.1
tiktoken==0.5.2

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# 工具库
httpx==0.25.2
python-dotenv==1.0.0
pyyaml==6.0.1
tenacity==8.2.3
circuitbreaker==2.0.0

# 对象存储
minio==7.2.0

# 监控
prometheus-client==0.19.0

# 其他
numpy==1.26.2
aiofiles==23.2.1
```

---

## 7. 数据库配置

### 7.1 使用 Docker 启动服务

```bash
# 启动所有依赖服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

### 7.2 docker-compose.yml

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: autonomind
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:6333/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: admin
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
  qdrant_data:
  rabbitmq_data:
  minio_data:
```

### 7.3 初始化数据库

```bash
# 运行数据库迁移
alembic upgrade head

# 或使用脚本
python scripts/init_db.py
```

---

## 8. 环境变量

### 8.1 创建 .env 文件

```bash
cp .env.example .env
```

### 8.2 .env 示例

```env
# 应用配置
APP_NAME=AutonoMind
APP_VERSION=1.0.0
DEBUG=True
ENVIRONMENT=development

# 服务器配置
HOST=0.0.0.0
PORT=8000
WORKERS=4

# 数据库配置
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/autonomind
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=10

# Redis 配置
REDIS_URL=redis://localhost:6379/0
REDIS_MAX_CONNECTIONS=20

# Qdrant 配置
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_API_KEY=
QDRANT_COLLECTION=knowledge_vectors

# RabbitMQ 配置
RABBITMQ_URL=amqp://admin:admin@localhost:5672/
RABBITMQ_QUEUE_PREFIX=autonomind

# MinIO 配置
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=autonomind
MINIO_SECURE=False

# LLM 配置
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=2000

# 认证配置
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# 日志配置
LOG_LEVEL=INFO
LOG_FORMAT=json

# 限流配置
RATE_LIMIT_PER_MINUTE=100

# 文件上传配置
MAX_UPLOAD_SIZE=104857600  # 100MB
ALLOWED_FILE_TYPES=pdf,txt,md,json

# Celery 配置
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2
CELERY_TASK_SERIALIZER=json
CELERY_RESULT_SERIALIZER=json
CELERY_TIMEZONE=Asia/Shanghai
```

---

## 9. 启动开发服务

### 9.1 启动后端服务

```bash
# 方式1: 使用 uvicorn
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 方式2: 使用脚本
./scripts/start_dev.sh

# 方式3: 使用 docker-compose
docker-compose up -d api
```

### 9.2 启动 Celery Worker

```bash
# 启动 Celery worker
celery -A app.tasks.celery_app worker --loglevel=info

# 启动 Celery beat(定时任务)
celery -A app.tasks.celery_app beat --loglevel=info
```

### 9.3 访问服务

| 服务 | 地址 | 说明 |
|------|------|------|
| **API 服务** | http://localhost:8000 | 后端 API |
| **API 文档** | http://localhost:8000/docs | Swagger UI |
| **API 文档** | http://localhost:8000/redoc | ReDoc |
| **RabbitMQ** | http://localhost:15672 | 消息队列管理(用户名:admin/密码:admin) |
| **MinIO** | http://localhost:9001 | 对象存储管理(用户名:minioadmin/密码:minioadmin) |

---

## 10. 开发工具配置

### 10.1 VS Code 配置

```json
{
  "editor.formatOnSave": true,
  "python.defaultInterpreterPath": "${workspaceFolder}/venv/bin/python",
  "python.linting.enabled": true,
  "python.linting.pylintEnabled": true,
  "python.linting.flake8Enabled": true,
  "python.testing.pytestEnabled": true,
  "python.testing.pytestArgs": ["tests"]
}
```

### 10.2 Git 钩子

```bash
# 安装 pre-commit
pip install pre-commit

# 初始化
pre-commit install

# 手动运行
pre-commit run --all-files
```

`.pre-commit-config.yaml`:

```yaml
repos:
  - repo: https://github.com/psf/black
    rev: 23.12.0
    hooks:
      - id: black

  - repo: https://github.com/pycqa/flake8
    rev: 6.1.0
    hooks:
      - id: flake8

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.7.1
    hooks:
      - id: mypy
        additional_dependencies: [pydantic]
```

### 10.3 测试配置

```bash
# 运行所有测试
pytest

# 运行测试并生成覆盖率报告
pytest --cov=app --cov-report=html

# 运行指定测试
pytest tests/test_api.py -v

# 运行集成测试
pytest tests/integration/
```

---

## 11. 常见问题

### 11.1 数据库连接失败

**问题**: `psycopg2.OperationalError: could not connect to server`

**解决**:
```bash
# 检查 PostgreSQL 是否运行
docker-compose ps postgres

# 查看日志
docker-compose logs postgres

# 重启服务
docker-compose restart postgres
```

### 11.2 依赖安装失败

**问题**: `pip install` 报错

**解决**:
```bash
# 升级 pip
pip install --upgrade pip

# 使用国内镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 11.3 端口被占用

**问题**: `Address already in use`

**解决**:
```bash
# 查找占用端口的进程
lsof -i :8000  # Linux/Mac
netstat -ano | findstr :8000  # Windows

# 杀死进程
kill -9 <PID>
```

### 11.4 Redis 连接失败

**问题**: `redis.exceptions.ConnectionError`

**解决**:
```bash
# 检查 Redis 是否运行
docker-compose ps redis

# 测试连接
redis-cli ping
```

---

**文档版本**: v1.0
**最后更新**: 2026-03-02
